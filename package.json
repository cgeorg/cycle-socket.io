{
  "name": "cycle-socket.io",
  "version": "2.0.1",
  "description": "A socket.io plugin for Cycle.js",
  "main": "dist/script.js",
  "files": [
    "dist"
  ],
  "repository": "https://github.com/cgeorg/cycle-socket.io",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/index.js --out-file dist/script.js",
    "prepublish": "npm run build"
  },
  "author": "Chris Marasti-Georg",
  "license": "ISC",
  "peerDependencies": {
    "socket.io-client": "^1.3.7",
    "xstream": "^5.2.1"
  },
  "devDependencies": {
    "babel": "^5.0.0"
  }
}
